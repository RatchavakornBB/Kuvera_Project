## Result: ✅ DoD met — Phase 2 (Core AI Loop) complete

Gate: full end-to-end HTTP verification, no mocks anywhere in the chain. Uploaded a real PDF via `curl`, POSTed `/deals/{id}/analyze`, got a real 200 with all 4 fields populated (summary 2,159 chars, 8 risk flags, 4,286-char IC memo, a real pricing note), confirmed the `analyses` row and the `documents.summary` column were both persisted. Also verified the failure path for real: a nonexistent `document_id` correctly returns HTTP 500 with `{"node":"doc_summarizer","attempts":2,"reason":"exhausted retries","raw_error":"No document with id ..."}` — not a generic unhelpful 500. A nonexistent deal correctly 404s before the graph ever runs.

Deviations from spec: none load-bearing.

Bug found and fixed mid-task (real, not hypothetical): both doc_summarizer and risk_flagger called `fetch_document()` (and risk_flagger also `get_last_analysis()`) *outside* their `with_retry` wrapper — only the model call itself was protected. A real failure at the fetch step (tested with a nonexistent document_id) leaked a raw `ValueError` straight through LangGraph and past the route's `except NodeFailure` handler, landing as FastAPI's generic unhelpful "Internal Server Error" — silently violating AGENT.md Section 10 despite the retry infrastructure existing. Fixed by moving the fetch calls inside the retried callable in both nodes, confirmed via the same failing request now returning the proper structured error, then re-ran the happy path to confirm no regression.

D-009: solved the cross-package import (`backend/app` calling `agents/`, a sibling directory, deferred from D-007) with a small `sys.path` bootstrap at the top of `backend/app/main.py` — inserts the repo root before other imports, so the app works regardless of which directory `uvicorn` was launched from. Considered documenting a specific `--app-dir backend` launch invocation instead (also works, tested), but chose the code-level fix since forgetting the right flags is an easy, silent way to break the demo — matches the project's general "fail fast / don't rely on remembering the right incantation" bias.

Risks: `/analyze` runs synchronously and took ~45s on this document (3 sequential-ish Claude calls: summarizer → risk flagger → the parallel ic_memo/pricing pair) — fine for the demo per the timeline's own "synchronously for the demo, no background queue needed at this scale" call, but worth knowing before a live demo moment where that wait needs a loading state in the UI (a later Phase 4 concern, not this task's).
