Scope: decision only — 5day-build-timeline.md's optional 15:30-17:00 block ("Optional: cloud
deploy... skip this block entirely if local + screen-share is sufficient").
Depends on: phase5-004 (done)

DoD:
  - [x] Asked the user directly rather than assuming a default, since this involves external
        services (Supabase cloud project, hosting) and only the user knows the actual interview
        logistics (screen-share vs. needing a shareable link)
  - [x] User decided: local + screen-share is sufficient. Cloud deploy explicitly skipped, not
        deferred/forgotten — this closes out the last open Phase 5 item.
